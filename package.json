{
  "name": "examhelper",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node --max-old-space-size=512 server.js"
  },
  "engines": {
    "node": ">=14.0.0 <19.0.0"
  }
}