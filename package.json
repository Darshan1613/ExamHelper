{
  "name": "examhelper",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd backend && npm install",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": ">=14.0.0 <19.0.0"
  }
}